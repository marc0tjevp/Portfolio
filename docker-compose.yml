version: "3"

services:
  portfolio:
    build: .
    # image: marc0tjevp/portfolio
    expose:
      - 80
    environment:
      VIRTUAL_HOST: n3rd.coffee
      LETSENCRYPT_HOST: n3rd.coffee
      LETSENCRYPT_EMAIL: marc0tjevp@gmail.com

networks:
  default:
    external:
      name: nginx-proxy
